({
    doInit : function(c, e, h){
       // h.doInit_Helper(c, e, h);
    },

    configPopup : function(c, e, h){
        h.configPopup_Helper(c, e, h);
    },

    refreshPage : function(c, e, h){
        h.viewReport_Helper(c, e, h);
    },

    closeConfigPopup : function(c, e, h){
        h.closeConfigPopup_Helper(c, e, h);
    },

    backtoRecord : function(c, e, h){
        h.backtoRecord_Helper(c, e, h);
    },

    saveConfig : function(c, e, h){
        h.saveConfig_Helper(c, e, h);
    },

    handleRadioClick : function(c, e, h){
        h.handleRadioClick_Helper(c, e, h);
    },

    viewReport : function(c, e, h){
        h.viewReport_Helper(c, e, h);
    },

    selectedReportConfig : function(c, e, h){
        h.selectedReportConfig_Helper(c, e, h);
    },

    showFilterList : function(c, e, h){
        h.showFilterList_Helper(c, e, h);
    },

    showReport : function(c, e, h){
        h.showReport_Helper(c, e, h);
    },
    showTooltip: function(c, e, h) {
        h.showTooltip_Helper(c, e, h);
    },
    hideTooltip: function(c, e, h) {
        h.hideTooltip_Helper(c, e, h);
    },

    showDomainTooltip : function(c, e, h) {
        h.showDomainTooltip_Helper(c, e, h);
    },
    hideDomainTooltip : function(c, e, h) {
        h.hideDomainTooltip_Helper(c, e, h);
    },
    editConfigName : function(c, e, h) {
        h.editConfigName_Helper(c, e, h);
    },
    saveConfigName : function(c, e, h) {
        h.saveConfigName_Helper(c, e, h);
    },
})